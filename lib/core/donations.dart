/// Donation link surfaced on the "Support this project" card (About
/// screen). Wird has no account/server/ads, so this is the only way to
/// support development.
///
/// PLACEHOLDER URL: replace with the real PayPal.me link before shipping
/// a public release, then add a matching `Donate:` field to
/// `metadata/io.github.ksulaiman579.wird.yml` (see RELEASE.md).
class DonationLinks {
  DonationLinks._();

  static const String payPal = 'https://paypal.me/REPLACE_ME';
}
